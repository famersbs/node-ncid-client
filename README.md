node-ncid-client
=============================================

Install

```
$ git add package.json index.js
$ git commit -m "prepare for 1.0.0"
$ npm version 1.0.0
v1.0.0  
$ git push && git push --tags
$ npm publish
```